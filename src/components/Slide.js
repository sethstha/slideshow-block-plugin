export default function Slide({ title, link }) {
	return (
		<div className="sethstha-slide">
			<a href={link} target="_blank">
				<h3>{title}</h3>
			</a>
			<figure></figure>
			GOod Good
		</div>
	);
}
